{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://164.90.145.135:8080/api/:path*"
    }
  ],
  "routes": [
    {
      "src": "^/(?!api).*$",
      "dest": "/index.html"
    }
  ]
}
